<Styles xmlns="https://github.com/avaloniaui"
		xmlns:ui="using:FluentAvalonia.UI.Controls"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Design.PreviewWith>
		<Border Padding="20">
			<!-- Add Controls for Previewer Here -->
			<CaptionButtons Classes="FluentCaptionsButton"/>
		</Border>
	</Design.PreviewWith>


	<Style Selector="CaptionButtons.FluentCaptionsButton">
		<Setter Property="Foreground" Value="Black"/>
	</Style>

	<!--<Style Selector="Button.FluentButton">
		<Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
		-->
	<!-- Reusing BorderBrush to define pressed background color, as it's not used otherwise -->
	<!--
		<Setter Property="BorderBrush" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
		<Setter Property="Width" Value="45"/>
		<Setter Property="VerticalAlignment" Value="Stretch"/>
		<Setter Property="Template">
			<ControlTemplate>
				<ContentPresenter Name="PART_ContentPresenter"
                                  Background="Transparent"
                                  Content="{TemplateBinding Content}"/>
			</ControlTemplate>
		</Setter>

		<Style Selector="Button.FluentButton:pointerover /template/ ContentPresenter">
			<Setter Property="Background" Value="{TemplateBinding Background}" />
		</Style>

		<Style Selector="Button.FluentButton:pressed /template/ ContentPresenter">
			<Setter Property="Background" Value="{TemplateBinding BorderBrush}" />
		</Style>
	</Style>-->
</Styles>